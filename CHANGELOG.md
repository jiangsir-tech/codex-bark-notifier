# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的基本格式，并计划采用[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Changed

- 超长会话改为流式读取，不再把整个 Codex 会话文件一次性载入内存；同时支持已归档会话和失效路径回退。
- 通知状态与正文会优先提取明确的回复、确认和继续操作要求，并清理内部状态标记、本地路径与空泛摘要。
- 优先使用 ChatGPT 或 Codex App 自带的兼容 Node.js，减少包管理器升级后通知失效的概率。
- 已发送去重记录只保留 90 天，审计日志达到 10 MB 后轮换，安装备份最多保留最近 5 份。

### Fixed

- 修复超大 JSONL 会话导致主任务识别超时、漏发通知或误判为内部任务的问题。
- 修复“告诉我结果”“确认后继续”等明确请求被误判为“本轮结束”的问题。
- 修复通知摘要为空、只显示“请回复”、保留内部隐藏标记或在完整确认事项中间截断的问题。

## [0.1.0] - 2026-08-01

### Added

- 通过 Bark 向 iPhone 推送 Codex 关键状态。
- 四种固定通知格式：本轮结束、需要你回复、受阻或出错、需要你批准。
- 官方 `agent-turn-complete` 外部通知事件支持。
- 官方 `PermissionRequest` Hook 支持，不改变正常批准流程。
- 主任务识别、子代理过滤与未知内部任务抑制。
- 基于 `thread-id + turn-id` 的持久化去重与陈旧锁恢复。
- 完成通知 5～15 秒动态延迟，批准通知即时发送。
- Codex 任务名称、本轮请求简称与回答摘要的提取、清理及长度限制。
- 回答摘要优先提取具体首句，跳过寒暄、空泛完成语和目录引导，并在完整标点处收束超长内容。
- 状态分类改为“回答摘要优先、末尾明确状态兜底”，并忽略引号中的引用示例、代码块与行内代码，避免把规则说明误认为真实的人机介入要求。
- Bark 自定义 Codex 风格图标、`Codex` 分组和 `minuet` 声音。
- 四类通知携带 ChatGPT 任务链接；点击后会尝试进入 Codex Remote，但 App、网页和具体任务的路由结果由 iOS 与 ChatGPT 决定。
- 私有密钥文件与最小化 JSONL 审计日志。
- macOS 交互式安装器、密钥文件自动化入口与卸载器。
- 自动发现 PATH、ChatGPT.app 或 Codex.app 内 Node.js 22+ 的安装引导脚本，并提供完整验证与单次测试推送入口。
- 面向 Mac 本地 Codex 的“仓库链接 + Device Key”便捷安装提示词，以及不把 Key 发进对话的隐藏输入流程。
- Bark “服务器”页面中 Device Key 所在位置的脱敏示意图。
- 安全策略、第三方标识声明和公开配置示例。

### Changed

- README 首页改为“用途、通知效果、优化与限制、安装、更新、卸载”的使用者路径，并增加直接复制给 Codex 的安装入口。
- 安装文档不再要求用户预先单独安装 Node.js 或 Codex CLI；安装流程优先复用桌面 App 自带运行时，并继续统一使用实际 `CODEX_HOME`。
- 真实 Bark 测试增加人工确认点；只有用户明确确认 iPhone 已显示通知，才报告手机收到。
- MIT 授权范围明确覆盖项目原创代码和原创文档，第三方名称、图标、商标及素材继续排除。
- 用户安装、更新和卸载提示固定使用 `v0.1.0` Tag，不再依赖会变化的 `main` 分支。
- README 明确 macOS 支持边界、Node.js 22/24 实测范围、公开配置项与需要修改源码的规则。

### Fixed

- 安装说明统一注明 `/hooks` 是 Codex CLI 的 Hook 管理入口，避免在桌面任务输入框中把它当成普通消息发送。
- 回答摘要和请求简称会隐藏“Bark Device Key 为……”等自然语言写法及较短的已标注 Bark Key，避免凭据进入完成或批准通知。
- Bark 请求在最终序列化前还会按本机实际 Device Key 做精确替换；即使 Key 使用点号、加号等合法字符并绕过文本启发式规则，也不会再次出现在通知标题或正文。
- 找不到与目标 `turn-id` 精确匹配的最终回复时，只使用该轮入队时保存的请求简称，不再读取后来轮次的用户消息作为兜底。
- `PermissionRequest` 在入队时固定本轮请求简称，并在最终格式化时再次执行凭据隐藏；只包含 Bark Key 的简称会安全回退。
- 安装、升级和卸载现在可识别 `--previous-notify` 包装链，只改写精确匹配的受管通知命令，同时保留 Computer Use 等外层工具及其参数。
- 安装清单丢失或受篡改时会递归检查通知链；遇到受管路径别名、格式异常或超深包装会安全拒绝，避免形成递归调用或在卸载后留下悬空通知命令。
- 升级时如果受管通知命令没有变化，会保留外层工具原始的 `--previous-notify` 字符串，避免无意义改写第三方配置格式。
- 安装和卸载使用同一 Codex Home 下的生命周期锁，并在写入前复核配置状态；并发修改发生时安全停止，不覆盖其他进程的新内容。
- 失败回滚只恢复仍与本项目刚写入内容一致的文件；检测到之后的并发修改时会保留它并明确报错。
- 异常退出遗留的生命周期回收声明会按进程、年龄和文件身份安全回收，不再永久阻断后续安装、更新或卸载。

### Security

- Device Key 安装模型分为两种：用户明确接受留存风险的便捷自动模式，以及只在独立 Terminal 隐藏提示中输入的高隐私模式。
- 便捷模式只允许把 Key 送入安装器隐藏 TTY 标准输入，不得放入命令参数、环境变量、URL、仓库、配置、Hook、日志或最终回复。
- Device Key 不写入源码、Codex 配置、Hook 命令、URL、日志、后台任务或磁盘回滚快照。
- Bark 服务地址默认必须使用 HTTPS，并拒绝 URL 凭据；只有显式开启时才允许 `localhost`、`127.0.0.1` 或 `::1` 的 HTTP，供本机联调。
- 文档明确正常配置下通知标题、摘要和 Device Key 会以普通 JSON 经 HTTPS 发往配置的 Bark 服务，首版尚未实现 Bark 内容加密。
- 后台任务使用严格最小 schema，并在等待或联网前删除。
- 安装和卸载回滚改为进程内快照，保留现有 Key 且不复制日志、状态或任务。
- `--key-file` 使用真实路径与单一硬链接校验，拒绝通过祖先目录符号链接或硬链接绕过仓库外限制。
- 临时 staging 不再写入已有通知器参数，避免参数中的凭据产生临时磁盘副本。
- `--purge` 在清单损坏或配置冲突时仍可清除固定私有数据，同时保持无法验证的 Codex 配置不变；部分删除会明确报告为不可逆。
- 安装、普通卸载和强制清理共用真实路径边界，拒绝经 `notifications` 或 `backups` 祖先符号链接访问外部目录。
- 强制清理逐项尝试运行目录、备份和旧版回滚目录，一个目标失败不会跳过其余目标。
- Hook 不自动批准或拒绝操作。
- 安装与卸载前备份配置，并保护已有通知器和无关 Hooks。

### Known limitations

- 首个版本仅面向 macOS 与 Node.js 22+。
- 使用桌面 App 内置 Node.js 时会持久化当时的绝对路径；App 被删除或内部目录变化后需要重新安装以更新路径。
- 无法保证 Codex、通知进程或操作系统硬崩溃时仍能发出通知。
- “需要回复”和“受阻或出错”依赖文本规则，可能误判或漏判。
- Codex 内部会话元数据不是稳定公共 API，增强解析可能随版本变化失效。
