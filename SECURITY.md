# Security Policy

## Supported versions

项目尚未发布首个稳定版本。安全修复目前只面向最新的 `main` 分支和最新 GitHub Release。旧版本可能不会获得补丁。

## 报告安全问题

请不要通过公开 GitHub Issue 报告可能泄露以下内容的问题：

- Bark Device Key；
- 本机用户名、绝对路径或私有仓库名称；
- Codex 对话正文、会话文件或审计日志原文；
- 可用于绕过 Hook 信任或自动批准操作的代码；
- 其他访问凭据。

优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。如果该入口尚未启用，请只创建不包含敏感信息的 Issue，说明“需要私密安全沟通”，等待维护者提供渠道。

报告中请包含：

- 受影响版本或 commit；
- 操作系统、Node.js 和 Codex 版本；
- 最小复现步骤；
- 预期行为与实际行为；
- 已脱敏的日志或测试事件；
- 影响范围及你已经采取的临时措施。

不要发送真实 Device Key。复现时使用专门创建、随后可重置的测试设备凭据。

## 凭据模型

Bark Device Key 可以用来向对应设备推送消息，应视为敏感凭据：

- 安装后的长期副本仅保存在安装器创建的私有密钥文件中；
- 文件权限应为 `600`，所在私有目录权限应为 `700`；
- 不进入任何 AI 对话或工具输入，包括运行在本机的 Codex；
- 不写入源码、Codex 配置、Hook 命令、URL、日志、后台任务或磁盘回滚快照；
- 不通过 `--key <secret>` 命令行参数传递；
- 不通过环境变量传递；
- 普通安装只允许用户在独立 Terminal 的隐藏输入提示中亲自输入；
- `--key-file` 只适用于非 AI 自动化；源文件的真实路径必须位于仓库外，不得是符号链接或硬链接，且只能由当前用户读取，并应在安装后删除；
- 怀疑泄露时，应立即在 Bark 中重置相关 Key，并更新本机安装。

## Hook 安全边界

`PermissionRequest` Hook 只发送通知：

- 不自动批准或拒绝；
- 成功时不输出决策；
- 不绕过 Codex Hook 信任；
- 不执行通知以外的外部操作。

用户应在 Codex CLI 的 `/hooks` 中检查真实命令和路径后再信任。

## 日志与隐私

本项目的审计日志只应记录时间、事件类型、线程/轮次标识、延迟及必要的失败原因。日志不得记录 Device Key、对话正文、最终回复或完整 HTTP 请求。

后台通知任务只允许保存严格校验后的最小通知数据。原始 Codex 事件、完整用户消息、完整助手回复和工具参数不得写入任务文件；交付进程应在等待或联网前先消费并删除任务文件。

完成类通知会把与该 `turn-id` 精确匹配、经格式清理、明显凭据隐藏和长度截断后的助手回复摘要发送到所配置的 Bark 服务；批准通知发送经清理和截断的本轮请求简称。若无法精确确认该轮最终回复，必须安全回退请求简称，不得读取其他轮次或尚未封口的中间消息。用户应据此选择公共 Bark 服务或自建服务。

如果提交故障报告，请再次检查并脱敏日志中的用户名、文件路径、线程标识和项目名称。

## 安装与卸载边界

安装器应备份并合并已有配置，不覆盖无关的 `notify` 与 Hooks。卸载器应只移除本项目添加的内容，不使用陈旧备份覆盖用户后来新增的设置。

在无法确认配置归属时，普通安装或卸载应停止并报告，而不是猜测性改写。用户明确执行 `--purge` 时，即使安装清单损坏或配置发生冲突，也只删除由固定路径推导出的私有运行目录、备份和严格匹配的旧版回滚目录，不修改无法安全恢复的 `config.toml` 或 Hooks。递归删除和旧版目录清理必须同时验证词法固定关系与基于最近已存在祖先的真实路径，不能经祖先符号链接越出 Codex Home。删除一旦开始即不可逆；单个目标失败后仍应继续尝试其余安全目标，并在最后明确报告部分失败。

安装和卸载的进程内回滚快照不得包含 Device Key、日志、状态或后台任务正文。临时 staging 目录也不得写入已有通知器参数，以免旧通知命令中的凭据产生额外磁盘副本。
