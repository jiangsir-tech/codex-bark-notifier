# Security Policy

## Supported versions

项目尚未发布首个稳定版本。安全修复目前只面向最新的 `main` 分支和最新 GitHub Release。旧版本可能不会获得补丁。

## 报告安全问题

请不要通过公开 GitHub Issue 报告可能泄露以下内容的问题：

- Bark Device Key；
- 本机用户名、绝对路径或私有仓库名称；
- Codex 对话正文、会话文件或审计日志原文；
- 可用于绕过 Hook 信任或自动批准操作的代码；
- 其他访问凭据。

优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。如果该入口尚未启用，请只创建不包含敏感信息的 Issue，说明“需要私密安全沟通”，等待维护者提供渠道。

报告中请包含：

- 受影响版本或 commit；
- 操作系统、Node.js 和 Codex 版本；
- 最小复现步骤；
- 预期行为与实际行为；
- 已脱敏的日志或测试事件；
- 影响范围及你已经采取的临时措施。

不要发送真实 Device Key。复现时使用专门创建、随后可重置的测试设备凭据。

## Bark Device Key 与两种安装模式

Bark Device Key 不是 Apple ID、ChatGPT 账号密码或设备解锁凭据，但持有者可以向对应 Bark 设备发送任意通知正文和点击链接。它是敏感的推送凭据：泄露通常不会让对方读取你的 Codex 对话，却可能被用于骚扰、伪造通知或诱导点击链接。怀疑泄露时，应在 Bark 中更换相关 Key，并更新本机安装。

项目提供两种明确不同的安装模式。

### 便捷自动安装

用户可以主动把自己的 Device Key 提供给 Mac 上的 Codex 本地任务，让 Codex 完成检查、测试、安装和一次测试推送。选择这种方式表示用户接受：

- Key 会进入当前 Codex 对话，并可能进入相关工具调用或服务端留存记录；“本地任务”不代表这些记录一定只保存在本机；
- 本项目无法验证、控制或删除 Codex 平台形成的对话及工具记录；
- Codex 只应把 Key 送入安装器的隐藏 TTY 标准输入，不得回显；若当前工具不能安全使用交互式 TTY，应退回由用户在独立 Terminal 亲自输入。

即使采用便捷模式，也不得把 Key 放入命令行参数、环境变量、URL、源码仓库、Codex 配置、Hook 命令、项目日志、后台任务或最终报告。安装后的长期副本只保存在安装器创建的私有密钥文件中；文件权限应为 `600`，所在私有目录权限应为 `700`。

### 隐藏输入安装

不希望 Key 进入 Codex 对话的用户，应选择隐藏输入模式。Codex 只完成预检和 `--dry-run`，然后由用户在独立 Terminal 的隐藏 TTY 提示中亲自输入 Key。此模式不得要求用户把 Key 发给 Codex，也不得通过 `--key <secret>`、环境变量、URL、仓库文件或普通临时文件传递。

`--key-file` 仅用于用户明确控制的高级自动化。源文件的真实路径必须位于仓库外，不得是符号链接或硬链接，只能由当前用户读取；调用方负责在不再需要时删除。安装器不会擅自删除用户提供的源文件。

公开 Issue、截图、日志样本和安全报告中始终不得包含真实 Device Key。复现问题时应使用专门创建且可以随后更换的测试凭据。

## Hook 安全边界

`PermissionRequest` Hook 只发送通知：

- 不自动批准或拒绝；
- 成功时不输出决策；
- 不绕过 Codex Hook 信任；
- 不写入或伪造信任状态；
- 不把用户 Hook 冒充为受管 Hook；
- 不使用 `--dangerously-bypass-hook-trust` 或等效方式代替用户审查；
- 不执行通知以外的外部操作。

安装器写入并启用 Hook 不等于 Hook 已被信任。用户应在 Codex CLI 的 `/hooks` 中检查真实的 `Event`、`Source`、`Command` 和路径后亲自信任。

## 日志与隐私

本项目的审计日志只应记录时间、事件类型、线程/轮次标识、延迟及必要的失败原因。日志不得记录 Device Key、对话正文、最终回复或完整 HTTP 请求。

后台通知任务只允许保存严格校验后的最小通知数据。原始 Codex 事件、完整用户消息、完整助手回复和工具参数不得写入任务文件；交付进程应在等待或联网前先消费并删除任务文件。

完成类通知会把与该 `turn-id` 精确匹配、经格式清理、明显凭据隐藏和长度截断后的助手回复摘要发送到所配置的 Bark 服务；批准通知发送经清理和截断的本轮请求简称。若无法精确确认该轮最终回复，必须安全回退请求简称，不得读取其他轮次或尚未封口的中间消息。用户应据此选择公共 Bark 服务或自建服务。

如果提交故障报告，请再次检查并脱敏日志中的用户名、文件路径、线程标识和项目名称。

## 安装与卸载边界

安装器应备份并合并已有配置，不覆盖无关的 `notify` 与 Hooks。卸载器应只移除本项目添加的内容，不使用陈旧备份覆盖用户后来新增的设置。

在无法确认配置归属时，普通安装或卸载应停止并报告，而不是猜测性改写。用户明确执行 `--purge` 时，即使安装清单损坏或配置发生冲突，也只删除由固定路径推导出的私有运行目录、备份和严格匹配的旧版回滚目录，不修改无法安全恢复的 `config.toml` 或 Hooks。递归删除和旧版目录清理必须同时验证词法固定关系与基于最近已存在祖先的真实路径，不能经祖先符号链接越出 Codex Home。删除一旦开始即不可逆；单个目标失败后仍应继续尝试其余安全目标，并在最后明确报告部分失败。

安装和卸载的进程内回滚快照不得包含 Device Key、日志、状态或后台任务正文。临时 staging 目录也不得写入已有通知器参数，以免旧通知命令中的凭据产生额外磁盘副本。
